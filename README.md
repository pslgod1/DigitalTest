## Установка
- Docker
- Docker Compose

### Запуск проекта
```bash
docker compose up -d --build

### Обновление проекта
```bash
docker compose down
docker compose up -d --build

### Остановка проекта
```bash
docker compose down
